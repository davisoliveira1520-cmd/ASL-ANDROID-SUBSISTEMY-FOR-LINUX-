name: shellcheck

on:
  push:
    paths:
      - 'scripts/**'
  pull_request:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ShellCheck todos os scripts
        uses: ludeeus/action-shellcheck@master
        with:
          severity: warning